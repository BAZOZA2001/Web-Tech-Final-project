spring.application.name=DataNavigatorFinalExam

# -----------------------------
# DATABASE CONFIGURATION
# -----------------------------
spring.datasource.url=jdbc:mysql://localhost:3306/datanavigator
spring.datasource.username=root
spring.datasource.password=Tresor92
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# -----------------------------
# HIBERNATE SETTINGS
# -----------------------------
spring.jpa.properties.hibernate.format_sql=true

# -----------------------------
# LOGGING CONFIGURATION
# -----------------------------
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springframework=INFO
logging.level.com.datanavigator=DEBUG

# -----------------------------
# THYMELEAF CONFIGURATION
# -----------------------------
spring.thymeleaf.cache=false
spring.thymeleaf.check-template-location=true

# -----------------------------
# MAIL CONFIGURATION
# -----------------------------
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dontresor922@gmail.com
spring.mail.password=tresor92
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# -----------------------------
# FILE UPLOAD CONFIGURATION (if applicable)
# -----------------------------
spring.servlet.multipart.max-file-size=2MB
spring.servlet.multipart.max-request-size=2MB

# -----------------------------
# CUSTOM APPLICATION PROPERTIES
# -----------------------------
# You can add any custom properties here if needed
